export async function searchPixabayPhotos(q,env,fetchJson){ const p=new URLSearchParams({key:env.PIXABAY_API_KEY,q:q.query,per_page:String(Math.max(3,Math.min(q.limit||8,200))),page:String(q.page||1)),safesearch:'true',image_type:'photo'}); if(q.orientation&&q.orientation!=='square')p.set('orientation',q.orientation==='portrait'?'vertical':'horizontal'); if(q.color)p.set('colors',q.color); const {data}=await fetchJson('https://pixabay.com/api/?'+p); return (data.hits||[]).map(x=>({provider:'pixabay',id:String(x.id),description:x.tags||'Pixabay image',width:x.imageWidth,height:x.imageHeight,orientation:x.imageWidth>x.imageHeight?'landscape':x.imageWidth<x.imageHeight?'portrait':'square',preview_url:x.webformatURL,asset_url:x.largeImageURL||x.webformatURL,source_page:x.pageURL,creator_name:x.user,creator_url:`https://pixabay.com/users/${x.user}-${x.user_id}/`,attribution:`Image by ${x.user} from Pixabay`,license_name:'Pixabay Content License',license_url:'https://pixabay.com/service/license-summary/'})); }
